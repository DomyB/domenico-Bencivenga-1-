source "https://rubygems.org"

# The same gem versions GitHub Pages uses to build the live site.
# Only needed if you want to preview the blog on your own computer.
gem "github-pages", "~> 232", group: :jekyll_plugins
